---
alwaysApply: true
---

Before finishing a component, ensure:

colors use semantic tokens (e.g. bg-primary, text-muted-foreground)
spacing uses defined scale (p-1, gap-2, etc.)
typography uses approved classes (text-body-sm, etc.)
sizes use semantic utilities (h-icon-16, h-control-sm, etc.)
no arbitrary values exist
